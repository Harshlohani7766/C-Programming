#include<stdio.h>
#include<conio.h>
void main()
{
int f,n,i;
int fib(int);
clrscr();
printf("enter no. of terms of fibonacci");
scanf("%d",&n);
for(i=0;i<n;i++)
{
f=fib(n);
}
printf("fibonacci=%d",f);
getch();
}
int fib(int n)
{
if(n==0 || n==1)
return(n);
else
return(fib(n-1)+fib(n-2));
}
